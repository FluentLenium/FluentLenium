# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: FluentLenium E2E pipeline

on: pull_request

jobs:
  e2etests:
    runs-on: ubuntu-latest
    steps:
    - name: Cache Maven packages
      uses: actions/cache@v2
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      env:
        BROWSERSTACK_GRID_URL: ${{ secrets.BROWSER_STACK_URL }}
      with:
        java-version: 11
    - name: E2E on Edge
      run: mvn clean package -DskipTests=true && mvn -Pexamples,java11 clean test -Dtest=*/examples/spring/* -DbrowserName=edge -Dmobile.simulator=false -DuseHub=true -DgridUrl=$BROWSERSTACK_GRID_URL
    - name: E2E on IE
      run: mvn clean package -DskipTests=true && mvn -Pexamples,java11 clean test -Dtest=*/examples/spring/* -DbrowserName=ie -Dmobile.simulator=false -DuseHub=true -DgridUrl=$BROWSERSTACK_GRID_URL
    - name: E2E on Chrome
      run: mvn clean package -DskipTests=true && mvn -Pexamples,java11 clean test -Dtest=*/examples/spring/* -DbrowserName=chrome -Dmobile.simulator=false -DuseHub=true -DgridUrl=$BROWSERSTACK_GRID_URL
    - name: E2E on Firefox
      run: mvn clean package -DskipTests=true && mvn -Pexamples,java11 clean test -Dtest=*/examples/spring/* -DbrowserName=firefox -Dmobile.simulator=false -DuseHub=true -DgridUrl=$BROWSERSTACK_GRID_URL
    - name: E2E on Iphone Web test (Safari on Iphone)
      run: mvn clean package -DskipTests=true && mvn -Pexamples,java11 clean test -Dtest=*/examples/spring/* -DbrowserName=iphone -Dmobile.simulator=false -DuseHub=true -DgridUrl=$BROWSERSTACK_GRID_URL
    - name: E2E on Android Web test (Chrome on Android device)
      run: mvn clean package -DskipTests=true && mvn -Pexamples,java11 clean test -Dtest=*/examples/spring/* -DbrowserName=android -Dmobile.simulator=false -DuseHub=true -DgridUrl=$BROWSERSTACK_GRID_URL
